const { clone, set } = require('general_utils');
const utils = require('general_utils');

const date = "스트링"
const a = [1, 1, 1, 2, 2]
const num = "aasfd1dfs"
// console.log(utils.sha256(num))
console.log(utils.getSignature())